<resources>

    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight">
        <!-- ... -->
    </style>

    <!-- Fullscreen theme. -->
    <style name="Theme.App.Fullscreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/Primary</item>
        <item name="colorOnPrimary">@color/OnPrimary</item>
        <item name="backgroundColor">@color/Primary</item>
        <item name="android:windowBackground">@color/Primary</item>

        <item name="android:statusBarColor">@color/OnPrimary</item>

        <item name="popupMenuStyle">@style/Widget.MaterialComponents.PopupMenu</item>
        <item name="android:contextPopupMenuStyle">@style/Widget.MaterialComponents.PopupMenu.ContextMenu</item>
        <item name="actionOverflowMenuStyle">@style/Widget.MaterialComponents.PopupMenu.Overflow</item>
    </style>

    <!--TEXT INPUT-->
    <style name="textInput" parent="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="startIconTint">@color/OnPrimary</item>
        <item name="android:textColorHint">@color/OnPrimary</item>
        <item name="endIconTint">@color/OnPrimary</item>
        <item name="hintTextColor">@color/OnPrimary</item>
    </style>

    <!--BUTTON-->
    <style name="button" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/OnPrimary</item>
        <item name="android:textColorHint">@color/OnPrimary</item>
        <item name="strokeColor">@color/OnPrimary</item>
    </style>

    <!-- Card layout. -->
    <style name="Widget.App.Card" parent="Widget.MaterialComponents.CardView">
        <!-- ... -->
    </style>


</resources>
