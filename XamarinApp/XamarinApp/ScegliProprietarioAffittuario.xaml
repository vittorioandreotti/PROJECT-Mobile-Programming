<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XamarinApp.ScegliProprietarioAffittuario"
             Padding="50">
    <ContentPage.Content>
        <StackLayout
            Margin="0,100,0,0">
            <Label 
                Text="Sono un:"
                FontSize="20"
                VerticalOptions="Start" 
                HorizontalOptions="CenterAndExpand"
                Margin=""/>
            <Button 
                Text="Proprietario (Locatore)" 
                Command="{Binding Proprietario}"/>
            <Button 
                Text="Affittuario (Coinquilino)" 
                Command="{Binding Affittuario}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>